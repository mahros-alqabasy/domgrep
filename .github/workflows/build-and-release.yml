name: Build and Release domgrep

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-deb:
    name: Build and Release .deb Package
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Git user (required for commit)
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

      - name: Install dependencies
        run: |
          sudo apt update
          sudo apt install -y dpkg-dev debhelper fakeroot

      - name: Bump version (patch by default)
        run: |
          chmod +x scripts/bump_version.sh
          ./scripts/bump_version.sh patch

      - name: Read new version
        id: version
        run: echo "version=$(cat VERSION)" >> $GITHUB_OUTPUT

      - name: Prepare Package Structure
        run: |
          mkdir -p pkg/DEBIAN
          mkdir -p pkg/usr/bin
          mkdir -p pkg/usr/share/man/man1
          mkdir -p pkg/usr/share/doc/domgrep

          cp debian/control pkg/DEBIAN/control
          cp src/*.sh pkg/usr/bin/
          cp man/domgrep.1 pkg/usr/share/man/man1/
          gzip -f pkg/usr/share/man/man1/domgrep.1
          cp debian/postinst pkg/DEBIAN/postinst
          chmod 755 pkg/DEBIAN/postinst
          cp README.md pkg/usr/share/doc/domgrep/

      - name: Build .deb package
        run: dpkg-deb --build pkg "domgrep_${{ steps.version.outputs.version }}.deb"

      - name: Upload Debian package artifact
        uses: actions/upload-artifact@v4
        with:
          name: domgrep-deb
          path: "domgrep_${{ steps.version.outputs.version }}.deb"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: "v${{ steps.version.outputs.version }}"
          release_name: "domgrep v${{ steps.version.outputs.version }}"
          body: "Automated release for domgrep version v${{ steps.version.outputs.version }}"
          files: "domgrep_${{ steps.version.outputs.version }}.deb"
          draft: false
          prerelease: false
          overwrite: true
